<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chart.js Example</title>
    <!-- Chart.jsライブラリの読み込み -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
      /* メインコンテナのスタイル */
      main {
        width: 100%;
        display: flex;
        justify-content: center;
      }
      /* グラフを表示するためのスタイル */
      #js-result {
        display: flex;
        flex-wrap: wrap;
        width: 50%;
        gap: 20px;
        justify-content: center;
      }
      /* キャンバス（グラフ）自体のスタイル */
      canvas {
        flex: 1 0 48%;
        max-width: 100%;
        height: 50vh;
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <main>
      <!-- グラフを表示するコンテナ -->
      <div id="js-result"></div>
    </main>
    <canvas id="dailyViewsChart"></canvas>

<script>

const chartLabels = <%= @chart_labels.to_json.html_safe %>; 


const chartData = <%= @chart_data.to_json.html_safe %>; 


// ----------------------------------------------------
// ----------------------------------------------------
const ctx = document.getElementById('dailyViewsChart').getContext('2d');
const myChart = new Chart(ctx, {
    type: "bar",
    data: {

      labels: chartLabels, 
      
      datasets: [{
        label: "日別アクセス合計数",
        data: chartData,
        

        backgroundColor: "rgba(54, 162, 235, 0.5)",
        borderColor: "rgba(54, 162, 235, 1)",
        borderWidth: 1,
      }],
    },
    options: {
      scales: {
        y: {
          beginAtZero: true,
          title: {
            display: true,
            text: 'アクセス数 (回)'
          }
        },
        x: {
          title: {
            display: true,
            text: '日付'
          }
        }
      },
    },
});
</script>
  </body>
</html>